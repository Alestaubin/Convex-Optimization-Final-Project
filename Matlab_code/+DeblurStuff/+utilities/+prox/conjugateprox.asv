%Calculating the conjugate of the prox
function output = conjugateprox(prox, y, t)
    arguments
        prox function_handle
        y (:,:,3) double
        t double
    end

    %As explained in t

